extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 PROJECTS

      p.lead.text-light
        | The projects below are from the Treehouse Fullstack JavaScript Techdegree program. Each project contains a link to the GitHub page and a live demo. Thanks!
  article.grid-container.portfolio-index
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- Loops over each project and creates the thumbnails.
      each project in projects
        .cell.Project
          a(href=`/project${project.id + 1}`)
            img.thumbnail(src=`${project.image_urls[0]}` alt=`${project.project_name} image`)
          h5 #{project.project_name}



